// Random dice = new Random();
// string result = dice.Next(0, 2) == 1 ? "heads" : "tails";
// Console.WriteLine(result);

// // int discount = saleAmount > 1000 ? 100 : 50;
// // Console.WriteLine($"Discount: {discount}");